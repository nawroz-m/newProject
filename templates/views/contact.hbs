<!DOCTYPE html>
<html>
    <head>
        <title>Contact</title>
        <link type="text/css" rel="stylesheet" href="css/styles.css">
    </head>
    
    <body>
        <div class="main-content">
            <h1>{{title}}</h1>
            {{>header}}
            <h3>{{contactText}}</h3>
        </div>
        
        {{>footer}}
    </body>
</html>