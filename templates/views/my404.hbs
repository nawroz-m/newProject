<!DOCTYPE html>
<html>
    <head>
        <title>404 </title>
        <link type="text/css" rel="stylesheet" href="css/styles.css">
    </head>
    <body>
        <div class="main-content">
            <h1>{{title}}</h1>
            {{>header}}
            <p>{{errorMessage}}</p>
        </div>
        
        {{>footer}}
    </body>
</html>